{
  "name": "habitar-o-corpo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "verify": "node scripts/verify-joelma-production.mjs",
    "build": "npm run verify && node -e \"const fs=require('node:fs');fs.rmSync('dist',{recursive:true,force:true});fs.cpSync('public','dist',{recursive:true});\""
  }
}
